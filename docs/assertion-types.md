# Assertion Types

CovenantAI supports various assertions for testing AI agent outputs:

- **Contains**: Checks if a string contains a substring.
- **Regex**: Checks if a string matches a regular expression.
- **JSON Validation**: Checks if output is valid JSON (coming soon).
